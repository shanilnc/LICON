{
  "name": "licon",
  "private": true,
  "workspaces": [
    "frontend",
    "admin",
    "backend"
  ],
  "scripts": {
    "dev:store": "npm run dev -w frontend",
    "dev:admin": "npm run dev -w admin",
    "dev:api": "npm run dev -w backend",
    "build": "npm run build -w frontend && npm run build -w admin",
    "test": "npm test -w backend",
    "seed": "npm run seed -w backend"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "prettier": "^3.9.9"
  }
}
